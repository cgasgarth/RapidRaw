# Release Docs

Release documentation is grouped by release responsibility:

- `process/` contains release process, limitations, update, and automation
  notes.
- `macos/` contains signing, packaging, and notarization docs.
- `policy/` contains user-facing privacy, telemetry, crash, and error policy.
- `evidence/` contains committed release evidence that is useful to review.
