name: PR Fast Validation

on:
  pull_request:
  workflow_dispatch:
    inputs:
      pull_request_number:
        description: Pull request receiving this required check
        required: false
        type: string
      base_sha:
        description: Immutable base commit used to plan affected lanes
        required: false
        type: string
      expected_head_sha:
        description: Immutable PR head that requested this run
        required: false
        type: string

concurrency:
  group: pr-fast-${{ github.event.pull_request.number || inputs.pull_request_number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  actions: read
  contents: read
  pull-requests: read

jobs:
  plan:
    name: 'validation: affected fast-lane plan'
    runs-on: ubuntu-latest
    timeout-minutes: 1
    outputs:
      js: ${{ steps.plan.outputs.js }}
      frontend: ${{ steps.plan.outputs.frontend }}
      schema: ${{ steps.plan.outputs.schema }}
      dependencies: ${{ steps.plan.outputs.dependencies }}
      rust: ${{ steps.plan.outputs.rust }}
      workflow: ${{ steps.plan.outputs.workflow }}
      docs: ${{ steps.plan.outputs.docs }}
      plan_json: ${{ steps.plan.outputs.plan_json }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0

      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: package.json

      - name: Collect changed paths
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha || inputs.base_sha }}
          EXPECTED_HEAD_SHA: ${{ inputs.expected_head_sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          set -euo pipefail
          if [[ -n "$EXPECTED_HEAD_SHA" && "$HEAD_SHA" != "$EXPECTED_HEAD_SHA" ]]; then
            echo "Expected PR head $EXPECTED_HEAD_SHA, got $HEAD_SHA." >&2
            exit 1
          fi
          if [[ -n "$BASE_SHA" ]]; then
            git diff --name-only --diff-filter=ACMR "$BASE_SHA...$HEAD_SHA" > .pr-paths
          else
            : > .pr-paths
          fi

      - name: Plan affected fast lanes and disclose deferred closure
        id: plan
        run: bun scripts/ci/pr-ci-contract.ts --paths-file .pr-paths

  fast-lint:
    name: 'pr fast: lint'
    needs: plan
    if: needs.plan.outputs.js == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: ./.github/actions/setup-bun-deps
      - run: bun run lint

  fast-typecheck:
    name: 'pr fast: typecheck'
    needs: plan
    if: needs.plan.outputs.js == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: ./.github/actions/setup-bun-deps
      - run: bun run typecheck

  fast-schema:
    name: 'pr fast: schema contracts'
    needs: plan
    if: needs.plan.outputs.schema == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: ./.github/actions/setup-bun-deps
      - run: bun run check:schema

  fast-contract-tests:
    name: 'pr fast: affected contract tests'
    needs: plan
    if: needs.plan.outputs.js == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: ./.github/actions/setup-bun-deps
      - name: Run bounded CI planner and native feedback contract tests
        run: bun test tests/pure-ts/ci/pr-ci-contract.test.ts tests/pure-ts/native-feedback.test.ts

  fast-format:
    name: 'pr fast: format'
    needs: plan
    if: needs.plan.outputs.js == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: ./.github/actions/setup-bun-deps
      - run: bun run format:check

  fast-build-i18n:
    name: 'pr fast: frontend build and i18n'
    needs: plan
    if: needs.plan.outputs.frontend == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: ./.github/actions/setup-bun-deps
      - name: Build frontend and validate translations
        run: |
          bun run check:bundle
          bunx i18next-cli lint
          bunx i18next-cli extract --ci --dry-run

  fast-unsafe-unused:
    name: 'pr fast: unsafe and unused dependency policy'
    needs: plan
    if: needs.plan.outputs.frontend == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: ./.github/actions/setup-bun-deps
      - name: Check script, cast, and dependency policy
        run: |
          bun tests/integration/checks/check-script-extension-policy.ts
          bun tests/integration/checks/check-unsafe-casts.ts
          bunx knip --config knip.jsonc --dependencies --reporter compact

  fast-visual:
    name: 'pr fast: visual smoke'
    needs: plan
    if: needs.plan.outputs.frontend == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: ./.github/actions/setup-bun-deps
      - name: Run browser visual smoke
        run: |
          bunx playwright install chromium
          bun scripts/proofs/capture-visual-smoke.ts --scenario empty-library
          bun tests/integration/checks/check-browser-tauri-harness.ts

  fast-js-security:
    name: 'pr fast: JavaScript audit'
    needs: plan
    if: needs.plan.outputs.dependencies == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: ./.github/actions/setup-bun-deps
      - run: bun audit --audit-level moderate

  fast-rust:
    name: 'pr fast: affected native feedback'
    needs: plan
    if: needs.plan.outputs.rust == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1.17.0
        with:
          components: rustfmt
          rustflags: ''
          rust-src-dir: src-tauri
      - uses: ./.github/actions/setup-bun-deps
      - name: Run bounded native format and focused contract feedback
        run: |
          cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check
          cargo metadata --manifest-path src-tauri/Cargo.toml --locked --no-deps --format-version 1 > /dev/null
          bun test tests/pure-ts/native-feedback.test.ts
          bun tests/integration/checks/check-native-feature-leaves.ts

  fast-workflow:
    name: 'pr fast: workflow contracts'
    needs: plan
    if: needs.plan.outputs.workflow == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version-file: package.json
      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version: '1.25.3'
          cache: true
      - name: Validate workflows and pinned actions
        run: |
          bun run check:actions
          bun tests/integration/checks/check-github-action-pins.ts

  fast-docs:
    name: 'pr fast: documentation contracts'
    needs: plan
    if: needs.plan.outputs.docs == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: ./.github/actions/setup-bun-deps
      - name: Validate documentation
        run: bun run check:docs

  pr-ci-required:
    name: PR CI / required
    needs:
      - plan
      - fast-lint
      - fast-typecheck
      - fast-schema
      - fast-contract-tests
      - fast-format
      - fast-build-i18n
      - fast-unsafe-unused
      - fast-visual
      - fast-js-security
      - fast-rust
      - fast-workflow
      - fast-docs
    if: always() && (github.event_name == 'pull_request' || inputs.pull_request_number != '')
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: ./.github/actions/setup-bun-deps
      - name: Fail closed on lane failure, unexpected skip, or budget overrun
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          NEEDS_CONTEXT: ${{ toJSON(needs) }}
          PLAN_JSON: ${{ needs.plan.outputs.plan_json }}
        run: |
          set -euo pipefail
          JOBS_CONTEXT="$(gh api "repos/${{ github.repository }}/actions/runs/${{ github.run_id }}/jobs?filter=latest&per_page=100")"
          export JOBS_CONTEXT
          bun scripts/ci/pr-ci-contract.ts --verify
