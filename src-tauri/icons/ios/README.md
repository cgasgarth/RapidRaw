# iOS icon bundle

This directory is a generated iOS app-icon bundle. Keep the canonical
`AppIcon-*` filenames intact so Tauri and downstream Apple asset consumers can
resolve the set correctly.

Do not rename, flatten, or reorganize these files unless the icon generator and
all packaging references are updated with the same change.
